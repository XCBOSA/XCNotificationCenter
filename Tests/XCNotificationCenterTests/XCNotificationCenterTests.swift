import XCTest
@testable import XCNotificationCenter

final class XCNotificationCenterTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(XCNotificationCenter().text, "Hello, World!")
    }
}
